# Mars Explorer V25

- Fixed coordinate-point button visibility/functionality.
- Added a real modal overlay for coordinate entry; previous version had the modal markup but no CSS, so removing `hide` left it statically clipped inside the map.
- Added robust click binding and exposed modal controls on `window`.
- Added mobile-responsive coordinate form.
